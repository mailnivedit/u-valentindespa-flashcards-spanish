/**
 * Placeholder for the statistics dashboard (per-card / per-category stats, TODO Phase 5).
 * Renders a minimal shell until localStorage or in-memory stats are wired.
 */
export default function Stats() {
  return (
    <div>
      <h1>Statistics</h1>
      <p>Stats coming soon...</p>
    </div>
  );
}
