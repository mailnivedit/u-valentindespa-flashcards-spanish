/**
 * Placeholder for dedicated Quiz UI (MCQ / fill-in-the-blank per specification).
 * Routes currently use CategorySelectionPage at /quiz until Phase 4 is implemented.
 * Kept so imports and future routing can wire here without a large rename.
 */
export default function Quiz() {
  return (
    <div>
      <h1>Quiz Mode</h1>
      <p>Quiz selection coming soon...</p>
    </div>
  );
}
